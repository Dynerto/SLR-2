services:
  - type: web
    name: capteer-instruct-crawler-worker
    env: docker
    plan: starter
    autoDeploy: false
    envVars:
      - key: CRAWLER_API_TOKEN
        sync: false
      - key: PUBLIC_BASE_URL
        sync: false